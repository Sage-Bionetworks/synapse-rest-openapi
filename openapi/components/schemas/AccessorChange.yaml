description: 'Request for a single user change access to a resource.'
example:
  type: RENEW_ACCESS
  userId: '...'
properties:
  type:
    $ref: '../defintions/AccessType.yaml'
  userId:
    description: 'The ID of the user.'
    type: string
title: AccessorChange
type: object
